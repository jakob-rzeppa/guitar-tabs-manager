<div class="form">
	{{ form_start(form) }}
	{{ form_row(form.email) }}
	<div class="form-select">
		{{ form_row(form.roles) }}
	</div>
	{{ form_row(form.password) }}
	<button class=" btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>
