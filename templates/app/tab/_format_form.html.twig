<form class="format-form" method="post" action="{{ path('app_tab_format', {'id': tab.id}) }}" onsubmit="return confirm('Are you sure you want to format this tab?');">
	<button class="form-submit" type="submit">Format Tab</button>
</form>
